# =============================================
# Nginx Configuration (Reserved)
# =============================================
# Saat ini server Go menangani HTTPS secara langsung.
# Konfigurasi nginx ini disiapkan untuk penggunaan di masa depan
# sebagai reverse proxy jika dibutuhkan.
#
# Untuk mengaktifkan nginx sebagai reverse proxy:
# 1. Install nginx di web/Dockerfile
# 2. Uncomment konfigurasi di bawah
# 3. Ubah Go server listen ke port internal (misal 8080)
# 4. Jalankan nginx di entrypoint.sh
# =============================================

# worker_processes auto;
#
# events {
#     worker_connections 1024;
# }
#
# http {
#     # Redirect HTTP ke HTTPS
#     server {
#         listen 80;
#         server_name localhost;
#         return 301 https://$host$request_uri;
#     }
#
#     # HTTPS reverse proxy ke Go app
#     server {
#         listen 443 ssl;
#         server_name localhost;
#
#         ssl_certificate     /app/certs/cert.pem;
#         ssl_certificate_key /app/certs/key.pem;
#
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ciphers HIGH:!aNULL:!MD5;
#         ssl_prefer_server_ciphers on;
#
#         # Security headers
#         add_header X-Frame-Options DENY;
#         add_header X-Content-Type-Options nosniff;
#         add_header X-XSS-Protection "1; mode=block";
#         add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#         location / {
#             proxy_pass http://127.0.0.1:8080;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# }
