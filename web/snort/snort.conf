# =============================================
# SNORT 2.9 CONFIGURATION — Kamsis Web Server
# Mode: IDS + IPS (Intrusion Detection & Prevention)
# Tanpa dynamic preprocessor (compatible semua env)
# =============================================

# ---- Network Variables ----
ipvar HOME_NET any
ipvar EXTERNAL_NET any
ipvar HTTP_SERVERS any
ipvar SQL_SERVERS any

portvar HTTP_PORTS [80,443]
portvar MYSQL_PORTS 3306

# ---- Paths ----
var RULE_PATH /etc/snort/rules
var LOG_PATH /var/log/snort

# ---- DAQ Config ----
config daq: pcap
config daq_mode: passive

# ---- Logging ----
config logdir: /var/log/snort

# ---- Performance ----
config utc
config show_year
config checksum_mode: none

# ---- Classifications ----
config classification: web-application-attack,Web Application Attack,1
config classification: attempted-user,Attempted User Privilege Gain,1
config classification: attempted-dos,Attempted Denial of Service,2
config classification: attempted-recon,Attempted Information Leak,2
config classification: misc-activity,Misc activity,3
config classification: trojan-activity,Trojan Activity,1

# =============================================
# RULES
# =============================================

# 1. Rules buatan sendiri (SQLi, XSS, BruteForce, dll)
include $RULE_PATH/local.rules

# 2. Rules ICMP
include $RULE_PATH/icmp.rules

# 3. Rules komunitas (kosong, untuk nanti diisi)
include $RULE_PATH/community.rules